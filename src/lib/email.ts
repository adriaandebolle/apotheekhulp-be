export async function sendWelcomeEmail(to: string, password: string, role: 'apotheek' | 'assistent') {
  // TODO: implement with Resend (or chosen mail service)
  console.log(`[email stub] welcome email → ${to} (${role})`)
}
